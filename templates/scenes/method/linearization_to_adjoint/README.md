# Linearization to adjoint

Use this recipe when a paper evaluates many policy shocks for one scalar outcome. It fills the explanatory gap between an implicit equilibrium and an adjoint-computation diagram.

Replace the notation and signs with those in the paper. The recipe assumes a nonsingular local Jacobian and does not claim that the nonlinear equilibrium itself is solved by the adjoint.
