# Selected observation biography

Use this recipe after an aggregate comparison to explain why one observation changes value or rank. The selected ID must refer to the same economic object in every state and view.

Replace the synthetic CSV and GeoJSON, update their manifest hashes, and change `SELECTED_ID`. Avoid using the card to imply a mechanism unless the displayed states isolate that mechanism.
