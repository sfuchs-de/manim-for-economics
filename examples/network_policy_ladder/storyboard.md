# Storyboard

The example first builds the spatial input from locations to a neutral network and then to values. It rearranges the same objects into a map-scatter-rank comparison, moves them through two model states, and closes with one observation's biography.
